@model List<Recipe>

@{
    ViewData["Title"] = "Recipes";
}

@foreach (var recipe in Model)
{
    <div class="recipe-card">
        <partial name="_RecipeCardPartial" model="recipe" />
    </div>
}

<a asp-action="AddRecipe">Add a Recipe</a>
